<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_splash_screen"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/gradient_bg"
    android:fitsSystemWindows="true"
    android:gravity="center"
    tools:context=".SplashActivity">

    <ImageView
        android:id="@+id/iconsplashscreen"
        android:layout_width="wrap_content"
        android:layout_height="@dimen/splashscreen_logo"
        android:layout_centerHorizontal="true"
        android:layout_centerInParent="true"
        android:layout_centerVertical="true"
        android:adjustViewBounds="true"
        android:src="@drawable/app_logo"
        android:transitionName="logotransition" />

    <ProgressBar
        android:id="@+id/progressBar"
        style="@style/Widget.AppCompat.ProgressBar.Horizontal"
        android:layout_width="wrap_content"
        android:layout_height="@dimen/splashscreen_progressbar"
        android:layout_below="@id/iconsplashscreen"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="@dimen/margin_medium"
        android:progressDrawable="@drawable/splashscreen_progressbar" />


</RelativeLayout>
